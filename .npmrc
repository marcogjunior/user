# Set the registry (usually default, but good to be explicit)
registry=https://registry.npmjs.org/

# Authentication token for private packages (keep this secret!)
# This works for scoped and unscoped private packages
//registry.npmjs.org/:_authToken=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